useful commands

docker compose -f .\docker-compose.local.yml up -d postgres redis pgadmin
docker compose -f .\docker-compose.local.yml down -v --remove-orphans
uv run ruff check . --fix
uv run ruff format .
uv run alembic upgrade head
uv run pytest